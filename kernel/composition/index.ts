/**
 * @kernel/composition — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/composition"` resolves the full
 * composition contract.
 */
export * from "./interfaces";
