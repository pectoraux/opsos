/**
 * @kernel/conformance/scenarios — scenarios barrel.
 *
 * Exports the 25 built-in generic reference scenarios as both named constants
 * and a single `REFERENCE_SCENARIOS` array. The array is the canonical suite
 * every OpsOS protocol MUST pass before packaging.
 */
export * from "./reference-scenarios";
