/**
 * @kernel/conformance — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/conformance"` resolves the full
 * conformance & simulation framework contract.
 */
export * from "./interfaces";
