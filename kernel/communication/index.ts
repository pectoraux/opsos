/**
 * @kernel/communication — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/communication"` resolves the full
 * communication runtime contract.
 */
export * from "./interfaces";
