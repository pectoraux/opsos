/**
 * @kernel/integration-hub — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/integration-hub"` resolves the
 * full integration-hub contract.
 */
export * from "./interfaces";
