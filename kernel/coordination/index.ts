/**
 * @kernel/coordination — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/coordination"` resolves the full
 * coordination contract.
 */
export * from "./interfaces";
