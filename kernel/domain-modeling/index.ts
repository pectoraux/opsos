/**
 * @kernel/domain-modeling — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/domain-modeling"` resolves the
 * full domain-modeling contract.
 */
export * from "./interfaces";
