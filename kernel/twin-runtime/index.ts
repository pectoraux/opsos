/**
 * @kernel/twin-runtime — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/twin-runtime"` resolves the full twin
 * runtime contract.
 */
export * from "./interfaces";
