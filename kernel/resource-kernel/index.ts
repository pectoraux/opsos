/**
 * @kernel/resource-kernel — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/resource-kernel"` resolves the full
 * resource-kernel contract.
 */
export * from "./interfaces";
