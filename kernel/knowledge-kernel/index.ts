/**
 * @kernel/knowledge-kernel — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/knowledge-kernel"` resolves the
 * full knowledge-kernel contract.
 */
export * from "./interfaces";
