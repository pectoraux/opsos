/**
 * @kernel/projections — root entry. Re-exports the public interfaces barrel so
 * `import { ... } from "@kernel/projections"` resolves the full projections
 * contract (read-model engine for CQRS).
 */
export * from "./interfaces";
