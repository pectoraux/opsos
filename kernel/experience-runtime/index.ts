/**
 * @kernel/experience-runtime — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/experience-runtime"` resolves the
 * full experience-runtime contract.
 */
export * from "./interfaces";
