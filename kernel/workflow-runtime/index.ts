/**
 * @kernel/workflow-runtime — root entry. Re-exports the public interfaces
 * barrel so `import { ... } from "@kernel/workflow-runtime"` resolves the full
 * workflow-runtime contract.
 */
export * from "./interfaces";
