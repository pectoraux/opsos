/**
 * @kernel/organizations — root entry. Re-exports the public interfaces barrel
 * so `import { ... } from "@kernel/organizations"` resolves the full
 * organizations contract.
 */
export * from "./interfaces";
