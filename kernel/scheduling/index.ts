/**
 * @kernel/scheduling — root entry. Re-exports the public interfaces barrel so
 * `import { ... } from "@kernel/scheduling"` resolves the full scheduling
 * contract.
 */
export * from "./interfaces";
